#include<iostream>
#include<string>
using namespace std;
int main()
{
    int n,c=0;
    cin>>n;
    if(n>50)
        return 0;
    string a;
    cin>>a;
    for(int i=0;i<n;i++)
    {
        if(a[i]==a[i+1])
        {
            c++;
        }
    }
    cout<<c<<endl;
    return 0;
}
